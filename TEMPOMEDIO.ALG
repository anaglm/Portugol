Algoritmo "semnome"

Var

dist: inteiro
velo: inteiro
tempoM: real

Inicio

escreval("Digite o valor da distancia do percurso (km)")
leia(dist)

escreval("Digite o valor da velocidade (km/h)")
leia(velo)

tempoM<- dist/velo

escreval("O tempo medio do percurso é:")
escreval (tempoM)

Fimalgoritmo